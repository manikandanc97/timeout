# AI Provider Setup

Timeout HRM includes a multi-provider AI assistant layer.

Supported provider modes in the current build:

- Demo
- OpenAI
- Gemini
- Anthropic
- Groq
- Ollama

Provider selection is managed through the admin settings UI. API keys and provider-specific host settings should be stored in backend environment variables or admin settings records depending on your deployment flow.

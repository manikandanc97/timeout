# Backend

This package contains the Express API, Prisma schema, services, middleware, tests, and operational scripts for Timeout HRM.

## Main Directories

- `src/`
- `prisma/`
- `tests/`
- `scripts/`

## Common Commands

```bash
npm run dev -w backend
npm run db:migrate -w backend
npm run db:seed -w backend
```
